
class Navigator(object):
  """
  Navigator converts raw materials into a set of
  relatable coordinates.
  """

  def convertToPoint(self, rawMaterials):
    """
    It shouldn't matter what rawMaterials contains, but it
    probably does. Usually does? Maybe not.
    """
    for material in rawMaterials.split():
      coord = [bit for bit in list(material)]

      # Look for 32, unicode space

    # Convert rawMaterials to binary
    # Convert in byte sections back to decimal
    # Breakdown decimal to a set of 26x26 coordinates
    # Return coordinates
    return

  def __init__(self):
    return
