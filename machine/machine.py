
class Machine(object):
  def __init__():
    """
    This may need to do something, but for now
    it uhhhhh...doesn't
    """

  def requestService(self, serviceName, taskName):
    """
    Take the request as generically as possible and begin
    the arduous process of actually making something
    happen.
    returns completed product
    """
    # Vette service through a Beauracrat
    # Service is requested or denied
    # If accepted
      # Determine if skills are in-house or consultant, via meeting.
      # Determine if committee required, via meeting.
      # Process request, via meeting.
    # If denied
      # Wait
      # Communicate?